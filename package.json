{
    "name": "docker-login-action",
    "version": "2.0.0",
    "private": true,
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc --outDir ./lib --rootDir ./src && ncc build lib/login.js -o dist/",
        "test": "jest",
        "test-coverage": "jest --coverage"
    },
    "keywords": [
        "actions",
        "node",
        "setup"
    ],
    "author": "step-security",
    "license": "MIT",
    "dependencies": {
        "@actions/core": "^3.0.1",
        "@actions/io": "^2.0.0",
        "axios": "^1.18.0"
    },
    "devDependencies": {
        "@types/jest": "^29.0.0",
        "@types/node": "^22.0.0",
        "@vercel/ncc": "^0.44.0",
        "jest": "^29.0.0",
        "ts-jest": "^29.0.3",
        "typescript": "^5.8.0"
    }
}
